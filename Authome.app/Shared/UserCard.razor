@using Data

<AuthomeCard Title=@Title Icon="fa-user" >
	@ChildContent
</AuthomeCard>

@code {
	[Parameter]
	public User User { get; set; }

	[Parameter]
	public RenderFragment? ChildContent { get; set; }

	private string Title
	{
		get { return $"{User.FirstName} {User.LastName}"; }
	}
}